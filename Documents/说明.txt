1. U盘供电采用5V直连（中间有一个自恢复保险丝），所以在USB BSP相关的初始化代码中，删除了对供电引脚
的初始化。

2. TM1637采用5V供电。单片机的IO口模拟IIC与TM1637通信，单片机是3.3V的，其IO口高电平是3.3V的。两个
IIC模拟引脚已经接上拉电阻至5V了，所以IO口初始化时，要将IO口配置为开漏输出，而不是推挽输出。

3. 单设display模块，为了应用程序开发的方便。display模块基于TM1637模块。

4.